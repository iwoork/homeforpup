// Messaging feature exports
export { default as MessagesPage } from '../dashboard/pages/messages/page';

// Components
export { default as ComposeMessage } from './components/ComposeMessage';
export { default as MessageView } from './components/MessageView';
export { default as ReplyForm } from './components/ReplyForm';
export { default as ThreadsList } from './components/ThreadsList';

// Note: API routes are handled by Next.js directly and don't need to be exported
